# Rarog — instructions for Claude Code

The operating rules live in `AGENTS.md`, so Claude Code and Codex read the
same file and cannot drift apart. It is imported here:

@AGENTS.md

Nothing else belongs in this file. Add rules to `AGENTS.md`.
