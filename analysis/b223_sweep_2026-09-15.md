# B.2.2.3 — curvature sweep and the P6 profile on the `b2core` arm

**Status: registration frozen 2026-09-15 before any sweep point was run
(`f7b766c`); results appended below the line the same day. Decision: B.2.3.** Leaf: PLAN B.2.2.3 (class `V`). Ledger:
RAR-S73 (B.2.3's condition and prediction P6). Subject: the `b2core` arm
B.2.2.2 left, engine `308abe9` plus the diag-only instrument `f975eae`,
bench 13 **4,706,910 / EBF 2.391**, the five categorical switches at their
defaults. Condition: analysis/search_programme_2026-09-13.md §9, and PLAN
rule 4 (SPSA is conditional).

## Recipe

- **Binary:** a `b2core,tune` release build (PEXT, not PGO). Node-limited and
  depth-limited counts do not depend on speed. Its bench at the defaults must
  read 4,706,910 before any point is used.
- **Points:** each of the five coordinates at 0.5x, 0.75x, 1x, 1.5x and 2x of
  its default, the others at their defaults, set by UCI option, rounded half
  away from zero. The 1x point is the defaults, shared by all five.

  | Coordinate | Default | 0.5x | 0.75x | 1.5x | 2x | Range |
  |---|---:|---:|---:|---:|---:|---|
  | `CoreRfpLinear` | 50 | 25 | 38 | 75 | 100 | 0..=200 |
  | `CoreLmpSquare` | 1351 | 676 | 1013 | 2027 | 2702 | 256..=4096 |
  | `CoreFpBase` | 150 | 75 | 113 | 225 | 300 | −100..=500 |
  | `CoreLmrQuiet` | 2432 | 1216 | 1824 | 3648 | 4864 | 0..=6144 |
  | `CoreCorrUpdateSlope` | 148 | 74 | 111 | 222 | 296 | 32..=512 |

  Every point is inside its range, so the option setter clamps nothing.
- **Per point:** `bench 13` nodes, and WAC solved at 100,000 nodes by
  `tools/diag/fixed_budget_probe.py nodes 100000 src/wac.epd` with the point's
  option flag (`label=exe|Name=Value`; the probe stops on an option the engine
  rejects, and records the options in its JSON).
- **Classification, per coordinate, on the WAC column `w` over the five
  multipliers:**
  1. *flat* when `max(w) − min(w) ≤ 2`;
  2. otherwise *curved* when the maximum of `w` occurs only at interior
     multipliers (0.75x, 1x or 1.5x), and at neither 0.5x nor 2x;
  3. otherwise *monotone*, with the maximum reached at an edge.

  The bench column is reported beside it as the tree surface. It does not
  enter the classification.
- **Rule (PLAN B.2.2.3):** flat or monotone on all five coordinates sends B.2
  to B.2.4 at the defaults; any curved coordinate sends it to B.2.3 over the
  registered coordinate set, less the five categorical switches.
- **P6:** `bench 13` with `CoreCorrWeightCont2=0 CoreCorrWeightCont4=0`
  against the defaults. P6 (analysis §12) predicted a change of **< 3%**; its
  falsifier is a larger change, meaning a correction consumer is acting as a
  pruning signal. The continuation-correction admissions by remaining depth
  come from `bench 13` on a `b2core,diag` build at stride 1
  (`tools/diag/bench_counters.py`, counters `corr_cont{2,4}_admitted_*`).
  They are reported as shares by depth bucket against Manta's "concentrated
  at low remaining depth".

---

## Results

Run 2026-09-15. Binary `tools/results/b223-20260915/rarog-b223-core-tune.exe`
(`b2core,tune`, PEXT, source `f7b766c`, whose engine is `308abe9` plus the
diag-only `f975eae`), sha256 `89144E6374F43F14…`, `2.4.0+b2core`, bench 13
**4,706,910** at the defaults. WAC at 100k reads **204** at the defaults,
reproducing B.2.2's reading of the same tree. Every point's options are
recorded in its probe JSON and were asserted there. No option was rejected, and
every point moved the tree. Script, per-point JSON and summary:
`tools/results/b223-20260915/sweep.py`, `wac100k_*.json`,
`sweep_summary.json`.

| Coordinate | 0.5x | 0.75x | 1x | 1.5x | 2x | Shape |
|---|---|---|---|---|---|---|
| `CoreRfpLinear` (25 / 38 / 50 / 75 / 100) | 4,771,192 · **212** | 4,720,731 · 207 | 4,706,910 · 204 | 5,888,466 · 205 | 6,342,238 · 204 | monotone (max at 0.5x) |
| `CoreLmpSquare` (676 / 1013 / 1351 / 2027 / 2702) | 5,810,712 · 202 | 5,969,260 · **211** | 4,706,910 · 204 | 5,883,695 · 204 | 5,474,708 · 196 | **curved** |
| `CoreFpBase` (75 / 113 / 150 / 225 / 300) | 5,107,346 · **209** | 5,398,382 · **209** | 4,706,910 · 204 | 6,516,024 · 199 | 4,785,543 · 202 | monotone (max ties at 0.5x) |
| `CoreLmrQuiet` (1216 / 1824 / 2432 / 3648 / 4864) | 8,069,014 · 199 | 6,426,933 · **205** | 4,706,910 · 204 | 3,752,254 · 201 | 3,740,925 · 204 | **curved** |
| `CoreCorrUpdateSlope` (74 / 111 / 148 / 222 / 296) | 5,619,918 · 201 | 5,349,395 · **207** | 4,706,910 · 204 | 5,481,930 · 206 | 5,422,647 · 202 | **curved** |

Each cell is `bench 13` nodes · WAC solved at 100k.

**Decision by the frozen rule: three coordinates are curved, so B.2 goes to
B.2.3** over the registered coordinate set, less the five categorical
switches.

Readings that do not change the decision:

- **`CoreLmpSquare` is the clearest curve:** 211 at 0.75x against 202 and 196
  at the edges.
- **`CoreLmrQuiet` is the weakest:** its maximum of 205 is one solve above the
  2x edge's 204.
- **The bench column is not a smooth surface.** The defaults are the smallest
  tree on four of the five coordinates, and single steps move it by 0.3% to 71%
  in either direction. Bench nodes respond chaotically to small coordinate
  changes, which is why the rule reads the WAC column, not the tree.

## P6

| `bench 13` | Nodes | Change |
|---|---:|---:|
| defaults | 4,706,910 | — |
| `CoreCorrWeightCont2=0 CoreCorrWeightCont4=0` | 4,970,064 | **+5.59%** |
| `CoreCorrWeightCont2=0` only | 5,107,600 | +8.51% |
| `CoreCorrWeightCont4=0` only | 4,708,145 | +0.03% |

**P6 predicted < 3%. The reading is +5.59%, so its falsifier fires as
worded.** Nearly all of the change comes from the two-ply table. Given the
bench chaos above, where a 0.75x step on an unrelated coordinate moves the
tree by more than this, the reading does not by itself show that the
continuation correction acts as a pruning signal. Whether it is re-admitted is
left to review; calibration is appended at B.2.4 (§12).

Continuation-correction admissions by remaining depth, `bench 13` on
`rarog-b223-core-diag.exe` (`b2core,diag`, sha256 `25811818…`) at stride 1.
The run reads 4,706,910 nodes and 218,190 correction updates, and each bucket
set sums to its total:

| Table | Admitted | depth 1 | 2 | 3 | 4–6 | 7+ |
|---|---:|---:|---:|---:|---:|---:|
| two plies back | 201,304 | 40.0% | 30.0% | 11.4% | 14.2% | 4.3% |
| four plies back | 184,799 | 41.9% | 30.4% | 10.9% | 13.4% | 3.3% |

**Concentrated at low remaining depth, as Manta measured:** 70.0% and 72.3% of
admissions train at depth 2 or less, and under 5% at depth 7 or more. The
two-ply context is present at 92.3% of correction updates.

Deviation on record: the first sweep attempt sent `quit` after `bench` and
the engine exited before printing, so the defaults assertion stopped it
before any point was measured. The rerun without `quit` is the result.
