# B.3 — proof searches and extensions on the fitted B.2 head

**Status: research packet for PLAN B.3, 2026-09-23; the implementation
contract for B.3.1 and the registered numbers for B.3.2 (RAR-S79).** B.0's
handoff (`search_programme_2026-09-13.md` §3.4, §3.5, §13.3) froze the
mechanisms on 2026-09-13 against the B.1 head. Since then B.2 rebuilt the
node around them, RAR-S75 fitted 82 constants (+139 Elo over the seeds), and
the head is 7,185,678 / EBF 2.444. This packet re-bases every activation
number on that head, re-reads the donors, and fixes what B.3.1 builds.

## Decision needed

Which of B.0's four B.3 mechanisms still earn their place on the fitted head,
in what form, with what seeds, and what B.3.2 must show before the SPSA and
the gate.

## Known evidence

**Activation on the head, `bench 13` at stride 1 (`counters_fitted_stride1`,
2026-09-23; interior nodes 3,869,032 of 7,185,678):**

| Mechanism | Head (fitted) | B.2.2 unfitted arm (2,285,006 interior) | Reading |
|---|---:|---:|---|
| `nmp_attempt` / `nmp_cut` | 488,901 / 107,464 = **22.0%** (12.6% of interior nodes attempt) | 231,551 / 74,510 = 32.2% | The fit made NMP cheaper to enter and worse at converting. Both donors gate NMP at cut nodes with a margin *above* beta; Rarog gates at `!tt_pv` with a margin *below* beta. |
| `nmp_verify_attempt` / `_fail` | 4,425 / **15** (0.3%) | 2,161 / 3 | The verification search disables the null move only at its own node, so it re-enables it one ply down and almost never disagrees. The donors disable it for the whole region (`nmp_min_ply`). Today's verification is inert. |
| `probcut_nodes` / `_attempt` / `_qpass` / `_cut` | 355,294 / 78,390 / 51,166 / 50,242 | 148,072 / 33,898 / 25,861 / 25,418 | **98.2% of qsearch passes survive the depth − 4 verification.** The verification confirms rather than checks. The donor deepens it exactly when the qsearch margin is thin. |
| `singular_attempt` → one / two / multicut / negative / none | 189,351 → 26% / 20% / 15% / 12% / 27% | 92,204 → 25% / 18% / 15% / 11% / 31% | Attempts 4.9% of interior nodes (4.0% unfitted). Rarog's margin is `4·d`; both donors use about `1·d` in their units, about `0.5·d` here. |
| `iir_applied` (no TT move / shallow TT) | 477,301 = **12.3%** of interior nodes (369,305 / 107,996) | 186,050 = 8.1% | The fit deepened the tree; IIR fires more. Reckless has no IIR; Stockfish's fires at cut and PV nodes only, depth ≥ 6, no TT move. |
| `hindsight_up` / `_down` | 299,670 / 439,892 | 242,266 / 225,313 | Fitted in RAR-S75 (deepen threshold 2249 → 1924). Stays. |
| `rfp_cut` | 1,513,292 = 39.1% of interior nodes | 35.3% | B.2's; context only. |

**Zero-game baselines on the head, measured 2026-09-23
(`tools/results/b3-20260923/summary_head.json`, binary
`rarog-b24b-core-pext-pgo.exe`, sha256 `2A586F3B…`):**

| Screen | Head | B.2.2 unfitted | Oracle |
|---|---:|---:|---:|
| Branching 4–14, phase-4 suite | **1.700** | 1.605 | 1.736 |
| Nodes at depth 14 relative to the oracle | **1.90x** (9,117,287) | 1.58x | 1.0 |
| WAC solved at 100k nodes | **221** | 204 | 242 |
| WAC solved at 400k nodes | **260** | 245 | 267 |
| Median depth at 300k | 15.5 | 16 | 19 |
| Oracle best-move agreement at 300k | **41/50** | 35 | — |
| Canaries (oracle stable ≤ 6, solved stable at ≤ anchor + 2 and at 100k; `depthpv 10`) | **87/116** | 75 | 116 |
| WAC.001 `g3g6` | unsolved by depth 10 | depth 14 | 9 |

The head sits inside every window B.0 set and above every floor except the
canaries; the fit moved WAC at 100k by +17 and agreement by +6. The oracle
column reproduces B.0's exactly (50/50 best moves).

**Rarog's own history on these mechanisms:**

- RAR-S56/S57/S58 (2026-08): the ProbCut SEE-gap filter (4.7c) alone carries
  +15.56 ± 10.02 Elo; the hard `est >= beta` NMP entry alone is unresolved
  (+2.19 ± 14.17) and contributes −0.40 nElo in company; a zero-game sweep of
  the NMP entry constants leaves conversion flat at 27.6–28.8%. **The NMP
  entry *margin* is not where the strength is; the population (cut nodes)
  and the reduction/verification shape have never been tested here.**
- RAR-S31: `SingularTtDepthMargin` 2 beat 3 by +3.35 ± 2.44 on a tune binary;
  parked. It is a categorical inside this cluster, not a gate.
- RAR-S06: removing the in-check extension was worth +30.75; **no in-check
  extension** stays a rule.
- RAR-S13, RAR-S60: `cutoffCnt` as a standalone reduction was rejected twice;
  B.2 landed it as a producer with the LMR consumer, and its NMP consumer is
  this cluster's, inside the cluster's gate.
- RAR-S50: mechanisms that prune overlapping regions are antagonistic in
  company; the cluster is gated as one unit and ablated by bit afterwards.
- B.2.2's ablation on the unfitted arm: removing null move, ProbCut,
  IIR+hindsight or singular each moved WAC at 100k by −1 to +9 and the
  canaries by −5 to +3, all small next to razoring (+13 / +29).
- RAR-S75's calibration: donor constants transferred badly (+139 Elo from the
  fit), donor *shapes* transferred well (+52 unfitted). Seeds here follow the
  same rule as B.2: shape from the donor, magnitude from Rarog's own fitted
  value where one exists, otherwise the donor's converted value.

## Unknowns

1. Whether the cut-node-only NMP population loses the all-node cutoffs Rarog
   takes today (the head converts 22% of attempts; the oracle 83%).
2. Whether ProbCut's deeper marginal verification costs more than the
   confirmations it removes (98.2% survive today).
3. Whether the `4·d` singular margin is Rarog's optimum or a relic of the
   pre-B.2 tree; the donor's `~0.5·d` would double the attempts that extend.
4. Whether IIR at 12.3% of interior nodes is paying for itself on the fitted
   tree, where Reckless has none.
5. Whether adding the LMR singular-margin term disturbs RAR-S75's fitted LMR
   constants (it was a zero slot when they were fitted).

## Hypotheses

- **H1 (B.0's, kept):** the donor's proof-search shapes — NMP at cut nodes
  with a verification region and a bound shortcut, ProbCut with margin-scaled
  verification depth and a lerp return, singular with graded extensions,
  soft multi-cut, −3 negative extension and LDSE — recover a coherent share
  of the remaining selectivity deficit. Prediction: +8 ± 8 Elo fitted.
- **H2 (competing):** the head's gains came from the move-loop cluster and
  these four mechanisms are already near their optimum in Rarog's form; the
  cluster measures +0 ± 5 fitted. Discriminated by the gate; partially by
  the unfitted paired run (H2 predicts it at or below zero).
- **H3 (competing, mechanism-local):** the strength is in ProbCut's
  verification and NMP's population alone; singular's graded extensions are
  neutral at STC. Discriminated by B.3.2's bit ablation on the fixed-node
  screens and, if the paired run is positive but small, by a categorical
  2,000-game run with bit 6 off.

## Interaction map

| Producer (B.2, fitted) | Consumer here | Where it can duplicate or cancel |
|---|---|---|
| `estimated_score` (TT-refined corrected eval) | NMP margin and reduction, ProbCut TT/eval gate, LDSE | RFP already prunes 39% of interior nodes above beta; NMP now demands a margin *above* beta at cut nodes, so it competes for RFP's leftovers, not its population |
| `improvement`, `improving` | NMP margin and reduction, ProbCut beta and base depth | shared with RFP/LMP/LMR; a tuned RFP improvement term means NMP's improvement term must be a coordinate, not a fixed seed |
| `tt_pv` | NMP margin (+), singular margin doubling, IIR policy | today `tt_pv` *vetoes* NMP and ProbCut outright; the cluster turns the veto into a margin term (NMP) and a node-type gate (ProbCut: cut nodes) |
| `cutoff_count[ply+1]` | NMP margin (−, when < 2) | LMR consumes it too; the two consumers read the same count in opposite directions (fewer child cutoffs → cheaper null move, more LMR) — by design in the donor |
| correction magnitude | singular double/triple margins | RFP, futility, LMR already widen with it |
| ProbCut TT store (Lower at `probcut_depth + 1`, now variable) | singular admission (`tt_depth >= depth − margin`), NMP bound shortcut | RAR-S31's ProbCut-seeded singular population moves with the store depth; the depth-margin categorical (3 vs 2) is measured *after* the new store lands |
| singular `singular_score`, `tt_move_score` | LMR margin term (new consumer) | RAR-S75's LMR constants were fitted with this slot at zero; the term is a coordinate seeded at the donor's converted value and the curvature sweep includes its offset |
| `nmp_min_ply` region (new thread state) | NMP entry | must be per-thread and cleared per search; helpers verify independently |

Feedback loops: multi-cut and ProbCut returns are not stored as exact scores
(donor: ProbCut stores Lower, multi-cut stores nothing), so they change which
nodes ever get an exact entry; the LMR margin term trains history through the
re-search rule. B.3.2 reports activation *and* cutoff composition, as B.2.2
did.

## Cheapest discriminating tests

In order, each on the fixed-node screens before any game:

1. Off-arm identity: `--no-default-features`-style flag off reproduces
   7,185,678 / EBF 2.444 and every 40 per-position line.
2. Stride-1 counters on the candidate: the P3–P5 predictions below are the
   first thing that can fail, and they fail cheaply (a wiring defect shows
   as a conversion that did not move).
3. The bit ablation sweep (bits 2, 3, 4, 6) on WAC at 100k and the canaries:
   locates a defect to a mechanism.
4. Categoricals as baseline/A/B/A+B on the fixed-node screens plus
   time-to-depth: `CoreIirPolicy` {current, off, SF-form}, `CoreProbcutTtServed`
   {off, on}, `SingularTtDepthMargin` {3, 2}. Zero games choose the default;
   one 2,000-game categorical run only where the screens disagree.
5. The 2,000-game unfitted paired run against the head (maintainer-run): the
   one screen that clears the cluster for its fit.

## Prospective prediction (frozen 2026-09-23, before any exposure)

- **P1** unfitted paired run, 2,000 games: **+5 ± 12 Elo**; P(≤ −40) 0.10.
- **P2** fitted, after the SPSA, the gate: **+8 ± 8 Elo** (about +12 nElo);
  under `[0,3]`: H1 0.60, cap 0.25, H0 0.15.
- **P3** NMP: attempts fall by 40–60%, conversion rises from 22% to ≥ 35%,
  verification failures rise above 0.3% of verifications (the region makes
  them real). Falsifier: conversion below 28% means the population gate did
  not land or the margin sign is wrong.
- **P4** ProbCut: attempts fall by ≥ 30% (cut nodes, TT gate, quiet-TT-move
  veto); cuts per attempt stay ≥ 60%; qsearch-pass-to-cut survival falls
  below 95%. Falsifier: survival still ≥ 98% means the marginal deepening is
  not wired.
- **P5** Singular: attempts per interior node within ±30% of 4.9%; the
  negative-extension share rises to ≥ 20% of attempts (the cut-node
  condition); multi-cut 10–20%; triple extensions < 5%.
- **P6** Branching stays in [1.60, 1.80]; WAC at 100k ≥ 215 unfitted; no
  canary the head passes is lost.
- **P7** Time-to-depth ≤ 1.05x of the head: the cluster adds no per-node
  producer.
- **P8** IIR: `off` costs > 5% time-to-depth; the default stays `current` or
  becomes `SF-form`.

## Falsifiers and stop conditions

- P1 ≤ −40: a defect, back to `IMPLEMENTED`, the bit sweep names it.
- P3 or P4 falsified on the counters: wiring, fixed before any game.
- Gate H0 or cap: the cluster is rejected as a unit; PLAN rule 6 counts it;
  the ablation sweep record says which bits to carry to B.6.
- A floor failure never holds a candidate the paired run cleared (rule 8).

## Decision

**READY_FOR_IMPLEMENTATION, with the contract below.** B.0's mechanism
decisions stand; three things change on the new evidence: NMP's population
moves to cut nodes and its verification becomes a region (today's is inert);
ProbCut keeps Rarog's measured filter and cap and adopts the donor's
verification depth and return; the singular margin is seeded at Rarog's own
`4·d`, not the donor's `0.5·d`, with the sweep deciding whether it curves.
IIR becomes a categorical decided by zero-game screens. The gate uses the
default `[0,3]` bracket rather than B.0's `[0,5]`: the prior is moderate and
RAR-M10 shows `[0,5]` rejects a true +4 nElo the default would accept; no
game has been played, so the bracket is fixed here, before any.

## Implementation contract (B.3.1, class `I2`)

Feature `b3proof`, off by default until B.3.4 flips it; the off arm is
byte-for-byte the head's search (7,185,678 / EBF 2.444, 40/40 lines). Version
string `+b3proof` on the arm. Constants live in `CoreParams` under `Core*`
names as spins; categoricals are switches, never SPSA coordinates.

**T1 NMP** (`src/search/core/node.rs`, the block at the null-move guard):
- Entry: `cut_node && !in_check && excluded.is_null() && !potential_singularity
  && ply >= td.nmp_min_ply && nmp_material_ok && !is_loss(beta)
  && !is_win(est)` and `est >= beta + max(2, m)`, with
  `m = CoreNmpBase − CoreNmpDepth·depth + CoreNmpTtPv·tt_pv
  − CoreNmpImprovement·improvement/1024 − CoreNmpCutoff·(cutoff_count[ply+1] < 2)`,
  seeds 150 / 4 / 50 / 43 / 10 (Reckless 337 / 9 / 110 / 94 / 21 × 0.457).
  No null move when the TT bound is Lower and the TT move captures a piece of
  at least knight value. `nmp_material_ok` stays Rarog's.
- Reduction, in 1024ths of a ply:
  `R = (CoreNmpRBase + CoreNmpRImproving·improving + CoreNmpRDepth·depth
  + CoreNmpREval·clamp(est − beta, 0, CoreNmpRClamp)/128) / 1024`,
  seeds 4407 / 917 / 265 / 1044 / 542 (the eval slope is 477 per Reckless
  unit, so 477 / 0.457 per Rarog unit; the clamp 1187 × 0.457).
- Bound shortcut: the null search runs against `tt_score` instead of `beta`
  when the TT score is valid, below beta, the bound is Lower and
  `depth − 2 <= tt_depth`; a fail-high against that bound counts.
- Verification: below `CoreNmpVerifyDepth` (16) or inside a region, a
  fail-high returns; otherwise `reduced = depth / 2` if the score is below
  beta (bound case) else `depth − R`, set `td.nmp_min_ply = ply + 3·reduced/4`,
  search `[beta − 1, beta]` at `reduced` with the null move disabled for every
  node at ply below `nmp_min_ply`, then clear it. Return the null score only
  if the verification holds. Never return an unproven mate (keep Rarog's
  clamp to beta).
- `nmp_min_ply` is per-thread state on `ThreadData`, zero at every search
  start; helpers own their own.

**T2 ProbCut:**
- Entry: `cut_node && !is_win(beta) && excluded.is_null()`, TT gate: when a
  TT score is valid it must be `>= probcut_beta` and not decisive, otherwise
  `est >= beta`; no ProbCut when the TT move is a quiet move.
- `probcut_beta = beta + CoreProbcutBase − CoreProbcutImproving·improving`,
  seeds 180 (Rarog's fitted) / 39 (85 × 0.457).
- Keep the SEE-gap filter (`see_ge(mv, (probcut_beta − static_eval)·
  ProbCutSeeGapScale/100)`, floored at 0) and the searched-move cap, now one
  coordinate `CoreProbcutMoveCap` seeded 4 (today's 2 + 2 at cut nodes).
- Depth: `base = max(0, depth − 4 − improving)`; after a qsearch pass,
  `probcut_depth = clamp(base − (score − probcut_beta)/CoreProbcutDepthDiv, 0, base)`
  (seed 146 = 319 × 0.457); search at `probcut_depth` against
  `adjusted = min(INF, probcut_beta + CoreProbcutAdjust·(base − probcut_depth))`
  (seed 90 = 197 × 0.457); if it fails below `adjusted` while
  `probcut_beta < adjusted`, re-search at `base` against `probcut_beta`,
  otherwise `probcut_beta = adjusted`.
- On a cut: store Lower at `probcut_depth + 1` with the move; return the
  score if decisive, else `lerp(score, beta, CoreProbcutLerp/1024)` (seed 276
  = 0.2695).
- `CoreProbcutTtServed` (switch, default 0): when 1, before the block, a TT
  Lower bound at `tt_depth >= depth − 4` with `tt_score >= beta +
  CoreProbcutTtMargin` (seed 208 = 428 × 0.485) and neither decisive returns
  that margin beta.

**T3 Singular, LDSE and the LMR term:**
- `potential_singularity = depth >= 5 + tt_pv && tt_depth >= depth −
  SingularTtDepthMargin && bound != Upper && valid && !decisive`, computed
  once per node (NMP consumes it).
- Margin: `k = CoreSingularMargin` (seed 4, Rarog's fitted `4·d`);
  `margin = (Exact ? ceil(depth/4) : depth)·k + depth·k·(tt_pv && !PV)`;
  `singular_beta = tt_score − margin`, `singular_depth = (depth − 1)/2`,
  exclusion search at the node's own `cut_node`.
- Below `singular_beta`: `extension = 1 + (score < singular_beta − double)
  + (score < singular_beta − triple)`, `double = CoreSingDoublePv·PV
  + CoreSingDoubleNotTtPv·(PV && !tt_was_pv) − CoreSingDoubleQuiet·quiet_tt
  − CoreSingDoubleCorr·|corr|/128`, seeds 89 / 22 / 7 / 7;
  `triple = CoreSingTriplePv·PV + CoreSingTripleNotTtPv·(PV && !tt_was_pv)
  − CoreSingTripleQuiet·quiet_tt − CoreSingTripleCorr·|corr|/128 +
  CoreSingTripleBase`, seeds 105 / 26 / 9 / 7 / 16. Cap +3.
- Multi-cut: `score >= beta && !decisive` returns `lerp(score, beta, 0.4027)`
  (`CoreSingMulticutLerp`, 412/1024); else if `score > tt_score` the TT move
  loses its first slot in the picker (searched later, not first); else if
  `tt_score >= beta || cut_node`, `extension = −3`.
- LDSE: when not a singular candidate, `depth <= 7 && !in_check && cut_node
  && est <= alpha − CoreLdseMargin` (seed 11) extends by one.
- LMR term: for every later move at a node where both `tt_move_score` (the
  TT move's returned score) and `singular_score` are valid,
  `r += clamp(CoreLmrSingularSlope·(tt_move_score − singular_score −
  CoreLmrSingularOffset)/128, 0, CoreLmrSingularCap)`, seeds 1085 / 85 / 2021
  (slope 496 / 0.457, offset 185 × 0.457).
- `new_depth = depth − 1 + extension` stays ≥ 1 by construction (candidates
  need depth ≥ 5); assert it in debug.

**T4 IIR policy:** `CoreIirPolicy` switch {1 current (default), 0 off,
2 Stockfish form: not at all-nodes, depth ≥ 6, no TT move only}. Hindsight
untouched.

**Invariants (tests):** off-arm fingerprint exact; no NMP at PV or all
nodes, in check, at excluded nodes, at the root, or at a ply below
`nmp_min_ply`; `nmp_min_ply` is zero after every verification and at search
start; a null fail-high never returns a mate; ProbCut never runs with a
decisive beta or a quiet TT move and stores a Lower bound with its move;
multi-cut never returns a decisive score; extension ∈ [−3, 3] and the child
depth ≥ 1; LDSE only at depth ≤ 7 cut nodes; the LMR term is zero when either
score is invalid; every new spin is advertised, at its default, inside its
range; correction training still refuses singular-excluded nodes under
`CoreCorrTrainExcluded = 0`.

**Counters (diag), stride 1:** keep the existing `nmp_*`, `probcut_*`,
`singular_*`, `iir_*` names where semantics are unchanged; add
`nmp_skip_region`, `nmp_bound_shortcut`, `nmp_verify_region_fail`,
`probcut_tt_gate_reject`, `probcut_quiet_tt_reject`, `probcut_deeper_research`,
`probcut_tt_served`, `singular_extend_three`, `singular_ttmove_demoted`,
`ldse_applied`, `lmr_singular_term`. Decision-trace lines for every new
decision with its inputs. `AblationMask` bits unchanged: 2 null move,
3 ProbCut, 4 IIR + hindsight, 6 singular (with LDSE and the LMR term).

**SPSA surface (B.3.3, if the sweep curves), 32 coordinates:** NMP margin 5,
reduction 5, verify depth 1; ProbCut base, improving, depth divisor, adjust,
lerp, cap (6) and the TT-served margin only if that switch is on; singular
margin 1, double 4, triple 5, multi-cut lerp 1, LDSE 1; LMR term 3. Curvature
sweep coordinates: `CoreNmpBase`, `CoreNmpREval`, `CoreProbcutBase`,
`CoreSingularMargin`, `CoreLmrSingularOffset`.

## Registered screens (B.3.2), against the head's numbers above

| Screen | Head | Floor | Target |
|---|---:|---:|---:|
| Branching 4–14 | 1.700 | window [1.55, 1.85] | inside |
| Nodes at depth 14 vs oracle | 1.90x | ≤ 2.2x | ≤ 1.7x |
| WAC at 100k | 221 | ≥ 215 | ≥ 226 |
| WAC at 400k | 260 | ≥ 255 | ≥ 263 |
| Oracle agreement at 300k | 41 | ≥ 39 | ≥ 43 |
| Canaries (`depthpv 10`) | 87/116 | none of the 87 lost | new passes recorded |
| Time-to-depth (bench 13 nodes / pooled-PGO NPS) | 1.00 | ≤ 1.10x | ≤ 1.00x |
| 2,000-game unfitted paired run | — | > −40 Elo | ≥ +5 proceeds to the sweep and SPSA; −40..+5 the review decides after the bit sweep |

The paired run governs (PLAN rule 8). The gate (B.3.4): Colosseum
`sprt-default` `[0,3]` nElo, cap 20,000 pairs (RAR-M10: a true +5 nElo
resolves in about 28,000 games, a true 0 in about the same), against the
accepted head at registration time — the B.2.4b head, or the B.2.7 head if
RAR-S78's gate accepts it first, in which case B.3's branch is rebased and
the zero-game screens and the paired run are repeated on the new base.
