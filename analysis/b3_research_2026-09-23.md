# B.3 — proof searches and extensions on the fitted B.2 head

**Status: research packet for PLAN B.3, 2026-09-23; the implementation
contract for B.3.1 and the registered numbers for B.3.2 (RAR-S79).** B.0's
handoff (`search_programme_2026-09-13.md` §3.4, §3.5, §13.3) froze the
mechanisms on 2026-09-13 against the B.1 head. Since then B.2 rebuilt the
node around them, RAR-S75 fitted 82 constants (+139 Elo over the seeds), and
the head is 7,185,678 / EBF 2.444. This packet re-bases every activation
number on that head, re-reads the donors, and fixes what B.3.1 builds.

## Decision needed

Which of B.0's four B.3 mechanisms still earn their place on the fitted head,
in what form, with what seeds, and what B.3.2 must show before the SPSA and
the gate.

## Known evidence

**Activation on the head, `bench 13` at stride 1 (`counters_fitted_stride1`,
2026-09-23; interior nodes 3,869,032 of 7,185,678):**

| Mechanism | Head (fitted) | B.2.2 unfitted arm (2,285,006 interior) | Reading |
|---|---:|---:|---|
| `nmp_attempt` / `nmp_cut` | 488,901 / 107,464 = **22.0%** (12.6% of interior nodes attempt) | 231,551 / 74,510 = 32.2% | The fit made NMP cheaper to enter and worse at converting. Both donors gate NMP at cut nodes with a margin *above* beta; Rarog gates at `!tt_pv` with a margin *below* beta. |
| `nmp_verify_attempt` / `_fail` | 4,425 / **15** (0.3%) | 2,161 / 3 | The verification search disables the null move only at its own node, so it re-enables it one ply down and almost never disagrees. The donors disable it for the whole region (`nmp_min_ply`). Today's verification is inert. |
| `probcut_nodes` / `_attempt` / `_qpass` / `_cut` | 355,294 / 78,390 / 51,166 / 50,242 | 148,072 / 33,898 / 25,861 / 25,418 | **98.2% of qsearch passes survive the depth − 4 verification.** The verification confirms rather than checks. The donor deepens it exactly when the qsearch margin is thin. |
| `singular_attempt` → one / two / multicut / negative / none | 189,351 → 26% / 20% / 15% / 12% / 27% | 92,204 → 25% / 18% / 15% / 11% / 31% | Attempts 4.9% of interior nodes (4.0% unfitted). Rarog's margin is `4·d`; both donors use about `1·d` in their units, about `0.5·d` here. |
| `iir_applied` (no TT move / shallow TT) | 477,301 = **12.3%** of interior nodes (369,305 / 107,996) | 186,050 = 8.1% | The fit deepened the tree; IIR fires more. Reckless has no IIR; Stockfish's fires at cut and PV nodes only, depth ≥ 6, no TT move. |
| `hindsight_up` / `_down` | 299,670 / 439,892 | 242,266 / 225,313 | Fitted in RAR-S75 (deepen threshold 2249 → 1924). Stays. |
| `rfp_cut` | 1,513,292 = 39.1% of interior nodes | 35.3% | B.2's; context only. |

**Zero-game baselines on the head, measured 2026-09-23
(`tools/results/b3-20260923/summary_head.json`, binary
`rarog-b24b-core-pext-pgo.exe`, sha256 `2A586F3B…`):**

| Screen | Head | B.2.2 unfitted | Oracle |
|---|---:|---:|---:|
| Branching 4–14, phase-4 suite | **1.700** | 1.605 | 1.736 |
| Nodes at depth 14 relative to the oracle | **1.90x** (9,117,287) | 1.58x | 1.0 |
| WAC solved at 100k nodes | **221** | 204 | 242 |
| WAC solved at 400k nodes | **260** | 245 | 267 |
| Median depth at 300k | 15.5 | 16 | 19 |
| Oracle best-move agreement at 300k | **41/50** | 35 | — |
| Canaries (oracle stable ≤ 6, solved stable at ≤ anchor + 2 and at 100k; `depthpv 10`) | **87/116** | 75 | 116 |
| WAC.001 `g3g6` | unsolved by depth 10 | depth 14 | 9 |

The head sits inside every window B.0 set and above every floor except the
canaries; the fit moved WAC at 100k by +17 and agreement by +6. The oracle
column reproduces B.0's exactly (50/50 best moves).

**Rarog's own history on these mechanisms:**

- RAR-S56/S57/S58 (2026-08): the ProbCut SEE-gap filter (4.7c) alone carries
  +15.56 ± 10.02 Elo; the hard `est >= beta` NMP entry alone is unresolved
  (+2.19 ± 14.17) and contributes −0.40 nElo in company; a zero-game sweep of
  the NMP entry constants leaves conversion flat at 27.6–28.8%. **The NMP
  entry *margin* is not where the strength is; the population (cut nodes)
  and the reduction/verification shape have never been tested here.**
- RAR-S31: `SingularTtDepthMargin` 2 beat 3 by +3.35 ± 2.44 on a tune binary;
  parked. It is a categorical inside this cluster, not a gate.
- RAR-S06: removing the in-check extension was worth +30.75; **no in-check
  extension** stays a rule.
- RAR-S13, RAR-S60: `cutoffCnt` as a standalone reduction was rejected twice;
  B.2 landed it as a producer with the LMR consumer, and its NMP consumer is
  this cluster's, inside the cluster's gate.
- RAR-S50: mechanisms that prune overlapping regions are antagonistic in
  company; the cluster is gated as one unit and ablated by bit afterwards.
- B.2.2's ablation on the unfitted arm: removing null move, ProbCut,
  IIR+hindsight or singular each moved WAC at 100k by −1 to +9 and the
  canaries by −5 to +3, all small next to razoring (+13 / +29).
- RAR-S75's calibration: donor constants transferred badly (+139 Elo from the
  fit), donor *shapes* transferred well (+52 unfitted). Seeds here follow the
  same rule as B.2: shape from the donor, magnitude from Rarog's own fitted
  value where one exists, otherwise the donor's converted value.

## Unknowns

1. Whether the cut-node-only NMP population loses the all-node cutoffs Rarog
   takes today (the head converts 22% of attempts; the oracle 83%).
2. Whether ProbCut's deeper marginal verification costs more than the
   confirmations it removes (98.2% survive today).
3. Whether the `4·d` singular margin is Rarog's optimum or a relic of the
   pre-B.2 tree; the donor's `~0.5·d` would double the attempts that extend.
4. Whether IIR at 12.3% of interior nodes is paying for itself on the fitted
   tree, where Reckless has none.
5. Whether adding the LMR singular-margin term disturbs RAR-S75's fitted LMR
   constants (it was a zero slot when they were fitted).
6. **Added by B.3.1, 2026-09-23, after T2 — a premise is false.** *Known
   evidence* reads the head's 98.2% qsearch-pass survival as "the
   verification confirms rather than checks" and says the donor "deepens it
   exactly when the qsearch margin is thin"; P4 predicts survival below 95%.
   The donor never verifies deeper than `depth − 4 − improving`, which is at
   most the head's `depth − 4`: it only verifies shallower, against a raised
   bound, when the margin is thick, and falls back to the base depth when that
   fails. And 16,626 of the head's 51,166 passes (32%) sit at depth 4, where
   the head's "verification" is the same qsearch run again and cannot fail;
   survival among passes verified by a real search is **97.3%** on the head
   and **97.9%** on the arm. P4's survival leg cannot be met by the contract's
   T2, and the falsifier's reading ("not wired") is refuted: the raised-bound
   path is live (below). Open: what T2's verification change is for, and
   what P4 should have said. **Resolved in amendment 2: option (a).**
7. **Added by B.3.1, 2026-09-23, after T1 — a premise does not hold at bench
   depth.** The null-move verification region refused **no** null move in
   `bench 13` (0 in 292 verifications at the seeds; 0 in 36,396 at
   `CoreNmpVerifyDepth = 4`; 0 with the depth-3 floor removed), and
   verification failed once in 292. Reverse futility answers the region's
   shallow nodes before the null move is asked, as the crossover note
   predicts for NMP generally; the region is live (9 refusals, 107 failures
   with the margin forced to 2). "The region makes them real" (P3) is not
   what happens; P3's 0.3% leg is met at 0.34% for another reason. Open:
   whether the region is kept as inert insurance for deeper game trees or
   dropped. **Resolved in amendment 2: kept, wire check moved to a depth-20 probe in B.3.2.**
8. **Added by B.3.1, 2026-09-23, after T3 — P5's activation legs contradict
   the contract's own formulas.** Every wire is live (each counter moved by a
   setting that should move it), and three of the resumption gates in
   amendment 2 fail:
   - **Attempts: 3.24% of interior nodes, below the band [3.43%, 6.37%].**
     The contract's candidate rule is the donor's `depth >= 5 + tt_pv`, where
     the head's in-loop rule is `depth >= 4`. Amendment 2's "the band already
     holds (5.9%)" was measured on the T1+T2 tree, which still ran the head's
     rule. With the floor alone set back to `depth >= 4` (a temporary diff),
     the arm reads 5.44%, inside the band.
   - **Three-ply extensions: 21.0% of attempts, against "< 5%".** Off the PV
     the contract's two-ply bar is `−7·quiet − 7·|corr|/128 ≤ 0`, so every
     non-PV extension is at least two plies, and its three-ply bar is 16 or
     less. Non-PV nodes carry 94% of the extensions and 99% of the triples.
     42% of extensions fall more than 20 units below the singular beta, the
     same share as the head's own two-ply extensions (43% at its margin of
     20). The < 5% leg could not hold under these seeds.
   - **Multi-cut: 32.2% of attempts, against 10–20%.** The contract's rule is
     the donor's `score >= beta`. Rarog's accepted rule requires the singular
     beta itself to reach beta, and the arm's multi-cuts meeting that rule
     are 19.3% of attempts, inside the band. The looser rule adds 12.9
     points.

   - **The arm fails an existing regression test.**
     `tests/endgames.rs::kbnk_positions_are_driven_to_mate` (the RAR-E14
     discriminator: mate within 90 plies at a majority of five node budgets)
     mates at only 2 of 5 on the T1–T3 arm, where T1+T2 passed. Disabling one
     T3 part at a time points at the donor's multi-cut rule. With multi-cut
     removed, or restricted to Rarog's rule, the test passes. With LDSE, the
     −3, the extra plies, the LMR term or the demotion removed it still
     fails.

   `ldse_applied` > 0 and `lmr_singular_term` > 0 hold. The
   negative-extension share (a read) is 27.7%. Open: whether P5's three legs
   become reads of a donor-faithful T3, or whether the depth floor or the
   multi-cut rule reverts to Rarog's. The KBNK failure makes the multi-cut
   question binding. The measurements are in *T3 record* at the end.

The measurements, wire proofs and the implementation's choices are in
*B.3.1 implementation record* at the end.

## Hypotheses

- **H1 (B.0's, kept):** the donor's proof-search shapes — NMP at cut nodes
  with a verification region and a bound shortcut, ProbCut with margin-scaled
  verification depth and a lerp return, singular with graded extensions,
  soft multi-cut, −3 negative extension and LDSE — recover a coherent share
  of the remaining selectivity deficit. Prediction: +8 ± 8 Elo fitted.
- **H2 (competing):** the head's gains came from the move-loop cluster and
  these four mechanisms are already near their optimum in Rarog's form; the
  cluster measures +0 ± 5 fitted. Discriminated by the gate; partially by
  the unfitted paired run (H2 predicts it at or below zero).
- **H3 (competing, mechanism-local):** the strength is in ProbCut's
  verification and NMP's population alone; singular's graded extensions are
  neutral at STC. Discriminated by B.3.2's bit ablation on the fixed-node
  screens and, if the paired run is positive but small, by a categorical
  2,000-game run with bit 6 off.

## Interaction map

| Producer (B.2, fitted) | Consumer here | Where it can duplicate or cancel |
|---|---|---|
| `estimated_score` (TT-refined corrected eval) | NMP margin and reduction, ProbCut TT/eval gate, LDSE | RFP already prunes 39% of interior nodes above beta; NMP now demands a margin *above* beta at cut nodes, so it competes for RFP's leftovers, not its population |
| `improvement`, `improving` | NMP margin and reduction, ProbCut beta and base depth | shared with RFP/LMP/LMR; a tuned RFP improvement term means NMP's improvement term must be a coordinate, not a fixed seed |
| `tt_pv` | NMP margin (+), singular margin doubling, IIR policy | today `tt_pv` *vetoes* NMP and ProbCut outright; the cluster turns the veto into a margin term (NMP) and a node-type gate (ProbCut: cut nodes) |
| `cutoff_count[ply+1]` | NMP margin (−, when < 2) | LMR consumes it too; the two consumers read the same count in opposite directions (fewer child cutoffs → cheaper null move, more LMR) — by design in the donor |
| correction magnitude | singular double/triple margins | RFP, futility, LMR already widen with it |
| ProbCut TT store (Lower at `probcut_depth + 1`, now variable) | singular admission (`tt_depth >= depth − margin`), NMP bound shortcut | RAR-S31's ProbCut-seeded singular population moves with the store depth; the depth-margin categorical (3 vs 2) is measured *after* the new store lands |
| singular `singular_score`, `tt_move_score` | LMR margin term (new consumer) | RAR-S75's LMR constants were fitted with this slot at zero; the term is a coordinate seeded at the donor's converted value and the curvature sweep includes its offset |
| `nmp_min_ply` region (new thread state) | NMP entry | must be per-thread and cleared per search; helpers verify independently |

**The reverse-futility crossover (checked 2026-09-23).** RFP runs before
NMP and returns at every non-`tt_pv` node with `est ≥ beta + margin`, and
B.2's fitted margin is `135·d²/16 + 17·d − 26` (not improving, no
correction, threatened): 101 at depth 3, 177 at 4, 270 at 5, 380 at 6, 650
at 8. NMP's population under the donor rule is the band
`beta + m ≤ est < beta + margin_RFP`; with the seed `m = 150 − 4d` that band
is **empty at depth 3** (138 > 101), 43 units wide at depth 4 and open from
depth 5. The donor is built the same way (Reckless RFP 313 against NMP 292
at depth 5, RFP 433 against 283 at 6), so NMP is a deep-node device in both
engines; at `tt_pv` nodes, where RFP is off, NMP runs alone with its +50.
Consequences: NMP attempts fall far more than the first P3 said; T1's
expected yield is small (the head's bit-2 ablation was already near-inert)
and its value is the verification region and the `cutoff_count` consumer;
`CoreNmpBase` is in the curvature sweep for exactly this. Rarog's own
population evidence (RAR-S57/S58 at `!tt_pv`, any node type) outranks the
donor's cut-node shape for both NMP and ProbCut, so each population is a
categorical with Rarog's as the default. The new LMR margin term lands on
LMR bases RAR-S75 fitted without it, so `CoreLmrQuiet`, `CoreLmrNoisy` and
`CoreLmrCutNode` join the B.3 surface. **Forward:** ProbCut's qsearch
verification and LDSE read today's quiescence; B.4 re-reads `probcut_qpass`
when it changes the quiescence.

Feedback loops: multi-cut and ProbCut returns are not stored as exact scores
(donor: ProbCut stores Lower, multi-cut stores nothing), so they change which
nodes ever get an exact entry; the LMR margin term trains history through the
re-search rule. B.3.2 reports activation *and* cutoff composition, as B.2.2
did.

## Cheapest discriminating tests

In order, each on the fixed-node screens before any game:

1. Off-arm identity: `--no-default-features`-style flag off reproduces
   7,185,678 / EBF 2.444 and every 40 per-position line.
2. Stride-1 counters on the candidate: the P3–P5 predictions below are the
   first thing that can fail, and they fail cheaply (a wiring defect shows
   as a conversion that did not move).
3. The bit ablation sweep (bits 2, 3, 4, 6) on WAC at 100k and the canaries:
   locates a defect to a mechanism.
4. Categoricals as baseline/A/B/A+B on the fixed-node screens plus
   time-to-depth: `CoreNmpNodes` {Rarog `!tt_pv` any type, cut nodes},
   `CoreProbcutNodes` {Rarog `!tt_pv` any type, Reckless cut nodes with the
   quiet-TT-move veto}, `CoreIirPolicy` {current, off, SF-form},
   `CoreProbcutTtServed` {off, on}, `SingularTtDepthMargin` {3, 2}. Zero games choose the default;
   one 2,000-game categorical run only where the screens disagree.
5. The 2,000-game unfitted paired run against the head (maintainer-run): the
   one screen that clears the cluster for its fit.

## Prospective prediction (frozen 2026-09-23, before any exposure)

- **P1** unfitted paired run, 2,000 games: **+5 ± 12 Elo**; P(≤ −40) 0.10.
- **P2** fitted, after the SPSA, the gate: **+8 ± 8 Elo** (about +12 nElo);
  under `[0,3]`: H1 0.60, cap 0.25, H0 0.15.
- **P3** NMP (amended the same day after the RFP crossover check, before
  any exposure): attempts fall by **≥ 60%** at the default population and
  more at cut nodes only; conversion rises from 22% to ≥ 35% (likely above
  50%: the band sits between two margins); verification failures rise above
  0.3% of verifications (the region makes them real). Falsifier: conversion
  below 28% means the margin did not land or its sign is wrong; attempts
  falling by less than 40% means RFP is not running first.
- **P4** ProbCut: at the default population attempts fall by 10–30% (the
  TT gate alone), at the Reckless population by ≥ 30%; cuts per attempt stay
  ≥ 60%; qsearch-pass-to-cut survival falls below 95%. Falsifier: survival still ≥ 98% means the marginal deepening is
  not wired.
- **P5** Singular: attempts per interior node within ±30% of 4.9%; the
  negative-extension share rises to ≥ 20% of attempts (the cut-node
  condition); multi-cut 10–20%; triple extensions < 5%.
- **P6** Branching stays in [1.60, 1.80]; WAC at 100k ≥ 215 unfitted; no
  canary the head passes is lost.
- **P7** Time-to-depth ≤ 1.05x of the head: the cluster adds no per-node
  producer.
- **P8** IIR: `off` costs > 5% time-to-depth; the default stays `current` or
  becomes `SF-form`.

## Falsifiers and stop conditions

- P1 ≤ −40: a defect, back to `IMPLEMENTED`, the bit sweep names it.
- P3 or P4 falsified on the counters: wiring, fixed before any game.
- Gate H0 or cap: the cluster is rejected as a unit; PLAN rule 6 counts it;
  the ablation sweep record says which bits to carry to B.6.
- A floor failure never holds a candidate the paired run cleared (rule 8).

## Decision

**READY_FOR_IMPLEMENTATION, with the contract below.** B.0's mechanism
decisions stand; three things change on the new evidence: NMP's population
moves to cut nodes and its verification becomes a region (today's is inert);
ProbCut keeps Rarog's measured filter and cap and adopts the donor's
verification depth and return; the singular margin is seeded at Rarog's own
`4·d`, not the donor's `0.5·d`, with the sweep deciding whether it curves.
IIR becomes a categorical decided by zero-game screens, and so do the NMP
and ProbCut populations, defaulting to Rarog's measured `!tt_pv` any-type
population over the donors' cut-node one. T1's expected yield is small (see
the crossover note); it stays for the verification region and the
`cutoff_count` consumer, and the bit sweep will say. The gate uses the
default `[0,3]` bracket rather than B.0's `[0,5]`: the prior is moderate and
RAR-M10 shows `[0,5]` rejects a true +4 nElo the default would accept; no
game has been played, so the bracket is fixed here, before any.

## Forward compatibility: what later phases consume from B.3, and what B.3 must not fix in place

Read from PLAN B.4–B.9, C, D, F and G on 2026-09-23. The rule for the
programme is that a later cluster consumes B.3 through the same few
interfaces B.2 established, never by editing B.3's mechanisms around its own
needs; so B.3 is built so that those interfaces are the only coupling.

| Later phase | What it changes that B.3 touches | What B.3 does now so that nothing has to be undone |
|---|---|---|
| **B.4 quiescence** (TT cutoff, corrected stand-pat, fail-high interpolation, LMP, SEE margin, exit TT write) | ProbCut's preliminary qsearch pass, NMP's deeply reduced null searches (which land in quiescence at shallow depth), LDSE's `est` at low depth | ProbCut's verification depth and adjusted beta are coordinates, not constants; `probcut_qpass`, `probcut_cut` and a depth-split `nmp_cut` counter keep their names so B.4 re-reads them; B.4's canary rule (quiet mate threats visible one ply later) already binds B.3's LMR term and negative extension, which reduce the non-TT moves at a singular node |
| **B.5 root, aspiration, optimism, PV** | `optimism` enters the corrected eval and therefore `est`, the input of every B.3 margin; aspiration windows put `beta` near mate scores more often | every B.3 consumer reads `est` through the node's one accessor (`eval_for_pruning`) and never `static_eval`, except the ProbCut SEE-gap filter, whose static-eval form is Rarog's measured one (RAR-S57); every guard on decisive scores uses the TB-bound helpers, so an aspiration `beta` in the mate range disables NMP, ProbCut, multi-cut and the shortcut by construction; the root is excluded from every B.3 mechanism by `NODE::ROOT`, which MultiPV relies on |
| **B.6 joint SPSA** | one tune over every coordinate the clusters left live | every B.3 constant is a `Core*` spin with a declared range; categoricals are resolved in B.3.2 and are switches, never coordinates (RAR-S13's lesson) |
| **B.7 speed pass** | behaviour-neutral throughput on the new modules | B.3 adds no per-node producer; `potential_singularity` is computed once per node from values the node already holds; no new allocation in the node |
| **B.8 cleanup** | dead parameters, unconsumed switches, the legacy search | the `b3proof` code lives as cfg-gated blocks *inside* `core/node.rs`, never a second node; when B.3.4 flips the default the cfg is deleted; the losing branch of each categorical and the legacy `SearchParams` names (`NullMoveDepthCoeff`, `NullMoveImprovingBonus`, `ProbCutMargin`, `ProbCutMoveCap*`, `SingularBetaMult`, `SingularDoubleMargin`, `NmpMinNonPawnPieces`) go with the legacy search |
| **B.9 checkpoint, ablation removed afterwards** | bit ablation on the accepted head | bits 2, 3, 4, 6 ablate the *new* forms whole (NMP with its region, singular with LDSE and the LMR term), so B.9's attribution reads the cluster it accepted |
| **C evaluation programme, C.10 search re-fit** | the evaluation's scale and error change; every eval-unit margin is mis-scaled until refit | margins are eval-unit coordinates seeded through the measured 0.457 ratio, with no scale constant baked into code; C.10 re-fits B.3's surface with the rest; correction magnitude keeps feeding the singular margins through the B.2 accessor |
| **D.1 time management, D threads** | node-count variance per move (verification and re-searches), `smp` reserve, helper threads | `nmp_min_ply` is per-thread state, zero at every search start; ProbCut and NMP touch the shared TT only through the existing store and probe API; nothing in B.3 reads or writes the clock |
| **F NNUE** | the evaluation again, smoother and on another scale; accumulators on make/unmake | B.3 adds no new make/unmake pattern (the null move and the excluded-move search already exist); the zugzwang guard stays Rarog's material rule so an NNUE endgame does not rely on a null-move verdict; F's re-fit covers the margins |
| **G.1 8–32 threads, NUMA** | TT contention, placement | no global state; ProbCut's Lower stores are the same one-entry writes the node already makes |

**What B.3 closes that B.2 left open on purpose.** Three B.2 producers get
their consumers here: `cutoff_count[ply+1]` in the NMP margin, ProbCut's TT
Lower store in singular admission (RAR-S31's population), and the singular
score in the LMR margin term. That is the sense in which the clusters are
meant to strengthen each other: B.2 chose its producers so that B.3 could
consume them without new state.

**Rules for the implementation (bind B.3.1):**

- R1 one `est`: every eval-unit comparison in T1–T3 reads `eval_for_pruning`;
  `static_eval` appears only in the ProbCut SEE-gap filter.
- R2 no fork: cfg-gated blocks inside `core/node.rs`; no second node, no
  duplicated helper; the diff is readable as "these blocks change".
- R3 no globals: `nmp_min_ply` and any new state live on the thread's data
  and are reset at search start; helpers verify independently.
- R4 the root is out: `NODE::ROOT` excludes every B.3 mechanism; MultiPV's
  identity at `MultiPV = 1` is asserted by the existing test.
- R5 names persist: counters keep their names where semantics are unchanged;
  new ones are listed in the contract; `nmp_cut` gains a depth split
  (`nmp_cut_d3_6`, `nmp_cut_d7_12`, `nmp_cut_d13_plus`) for B.4.
- R6 decisive scores: `is_win`, `is_loss`, `is_decisive` on `TB_WIN_SCORE`,
  as RFP does; no mechanism returns or stores a mate it did not prove.
- R7 depth order: IIR and hindsight adjust `depth` at entry; pruning reads the
  adjusted depth; singular, negative extension and LDSE act per move on it;
  the child depth is asserted ≥ 1 in debug builds.

## Implementation contract (B.3.1, class `I2`)

Feature `b3proof = ["b2core"]` (it requires the core; the legacy search
never compiles it), off by default until B.3.4 flips it; the off arm is
byte-for-byte the head's search (7,185,678 / EBF 2.444, 40/40 lines). Version
string `+b3proof` on the arm. Constants live in `CoreParams` under `Core*`
names as spins; categoricals are switches, never SPSA coordinates.

**T1 NMP** (`src/search/core/node.rs`, the block at the null-move guard):
- Population: `CoreNmpNodes` switch, 0 (default) = Rarog's measured
  population, `!tt_pv` at any node type; 1 = cut nodes only, the donors'.
  Below, `<population>` stands for whichever is selected.
- Entry: `<population> && !in_check && excluded.is_null() && !potential_singularity
  && ply >= td.nmp_min_ply && nmp_material_ok && !is_loss(beta)
  && !is_win(est)` and `est >= beta + max(2, m)`, with
  `m = CoreNmpBase − CoreNmpDepth·depth + CoreNmpTtPv·tt_pv
  − CoreNmpImprovement·improvement/1024 − CoreNmpCutoff·(cutoff_count[ply+1] < 2)`,
  seeds 150 / 4 / 50 / 43 / 10 (Reckless 337 / 9 / 110 / 94 / 21 × 0.457).
  No null move when the TT bound is Lower and the TT move captures a piece of
  at least knight value. `nmp_material_ok` stays Rarog's.
- Reduction, in 1024ths of a ply:
  `R = (CoreNmpRBase + CoreNmpRImproving·improving + CoreNmpRDepth·depth
  + CoreNmpREval·clamp(est − beta, 0, CoreNmpRClamp)/128) / 1024`,
  seeds 4407 / 917 / 265 / 1044 / 542 (the eval slope is 477 per Reckless
  unit, so 477 / 0.457 per Rarog unit; the clamp 1187 × 0.457).
- Bound shortcut: the null search runs against `tt_score` instead of `beta`
  when the TT score is valid, below beta, the bound is Lower and
  `depth − 2 <= tt_depth`; a fail-high against that bound counts.
- Verification: below `CoreNmpVerifyDepth` (16) or inside a region, a
  fail-high returns; otherwise `reduced = depth / 2` if the score is below
  beta (bound case) else `depth − R`, set `td.nmp_min_ply = ply + 3·reduced/4`,
  search `[beta − 1, beta]` at `reduced` with the null move disabled for every
  node at ply below `nmp_min_ply`, then clear it. Return the null score only
  if the verification holds. Never return an unproven mate (keep Rarog's
  clamp to beta).
- `nmp_min_ply` is per-thread state on `ThreadData`, zero at every search
  start; helpers own their own.
- A verification search runs at this ply and overwrites `stack[ply]`;
  restore `tt_pv` and `laterality` afterwards, as the singular search does.
- Score helpers on Rarog's own bounds: `is_win(s) = s >= TB_WIN_SCORE`,
  `is_loss(s) = s <= −TB_WIN_SCORE`, `is_decisive(s) = |s| >= TB_WIN_SCORE`,
  the convention RFP already uses.

**T2 ProbCut:**
- Population: `CoreProbcutNodes` switch, 0 (default) = Rarog's measured
  population (RAR-S57/S58), `!tt_pv` at any node type, no quiet-TT-move
  veto; 1 = Reckless's, cut nodes only with no ProbCut when the TT move is
  quiet.
- Entry: `<population> && !is_win(beta) && excluded.is_null() && !in_check`,
  TT gate on both populations: when a TT score is valid it must be
  `>= probcut_beta` and not decisive, otherwise `est >= beta`.
- `probcut_beta = beta + CoreProbcutBase − CoreProbcutImproving·improving`,
  seeds 180 (Rarog's fitted) / 39 (85 × 0.457).
- Keep the SEE-gap filter (`see_ge(mv, (probcut_beta − static_eval)·
  ProbCutSeeGapScale/100)`, floored at 0) and the searched-move cap, now one
  coordinate `CoreProbcutMoveCap` seeded 4 (today's 2 + 2 at cut nodes).
- Depth: `base = max(0, depth − 4 − improving)`; after a qsearch pass,
  `probcut_depth = clamp(base − (score − probcut_beta)/CoreProbcutDepthDiv, 0, base)`
  (seed 146 = 319 × 0.457); search at `probcut_depth` against
  `adjusted = min(INF, probcut_beta + CoreProbcutAdjust·(base − probcut_depth))`
  (seed 90 = 197 × 0.457); if it fails below `adjusted` while
  `probcut_beta < adjusted`, re-search at `base` against `probcut_beta`,
  otherwise `probcut_beta = adjusted`.
- On a cut: store Lower at `probcut_depth + 1` with the move; return the
  score if decisive, else `lerp(score, beta, CoreProbcutLerp/1024)` (seed 276
  = 0.2695).
- `CoreProbcutTtServed` (switch, default 0): when 1, before the block, a TT
  Lower bound at `tt_depth >= depth − 4` with `tt_score >= beta +
  CoreProbcutTtMargin` (seed 208 = 428 × 0.485) and neither decisive returns
  that margin beta.

**T3 Singular, LDSE and the LMR term:**
- `potential_singularity = depth >= 5 + tt_pv && tt_depth >= depth −
  SingularTtDepthMargin && bound != Upper && valid && !decisive`, computed
  once per node (NMP consumes it).
- Margin: `k = CoreSingularMargin` (seed 4, Rarog's fitted `4·d`);
  `margin = (Exact ? ceil(depth/4) : depth)·k + depth·k·(tt_pv && !PV)`;
  `singular_beta = tt_score − margin`, `singular_depth = (depth − 1)/2`,
  exclusion search at the node's own `cut_node`.
- Below `singular_beta`: `extension = 1 + (score < singular_beta − double)
  + (score < singular_beta − triple)`, `double = CoreSingDoublePv·PV
  + CoreSingDoubleNotTtPv·(PV && !tt_was_pv) − CoreSingDoubleQuiet·quiet_tt
  − CoreSingDoubleCorr·|corr|/128`, seeds 89 / 22 / 7 / 7;
  `triple = CoreSingTriplePv·PV + CoreSingTripleNotTtPv·(PV && !tt_was_pv)
  − CoreSingTripleQuiet·quiet_tt − CoreSingTripleCorr·|corr|/128 +
  CoreSingTripleBase`, seeds 105 / 26 / 9 / 7 / 16. Cap +3.
- Multi-cut: `score >= beta && !decisive` returns `lerp(score, beta, 0.4027)`
  (`CoreSingMulticutLerp`, 412/1024); else if `score > tt_score` the TT move
  loses its first slot in the picker (searched later, not first); else if
  `tt_score >= beta || cut_node`, `extension = −3`.
- LDSE: when not a singular candidate, `depth <= 7 && !in_check && cut_node
  && est <= alpha − CoreLdseMargin` (seed 11) extends by one.
- LMR term: for every later move at a node where both `tt_move_score` (the
  TT move's returned score) and `singular_score` are valid,
  `r += clamp(CoreLmrSingularSlope·(tt_move_score − singular_score −
  CoreLmrSingularOffset)/128, 0, CoreLmrSingularCap)`, seeds 1085 / 85 / 2021
  (slope 496 / 0.457, offset 185 × 0.457).
- `new_depth = depth − 1 + extension` stays ≥ 1 by construction (candidates
  need depth ≥ 5); assert it in debug.

**T4 IIR policy:** `CoreIirPolicy` switch {1 current (default), 0 off,
2 Stockfish form: not at all-nodes, depth ≥ 6, no TT move only}. Hindsight
untouched.

**Invariants (tests):** off-arm fingerprint exact; no NMP at PV or all
nodes, in check, at excluded nodes, at the root, or at a ply below
`nmp_min_ply`; `nmp_min_ply` is zero after every verification and at search
start; a null fail-high never returns a mate; ProbCut never runs with a
decisive beta or a quiet TT move and stores a Lower bound with its move;
multi-cut never returns a decisive score; extension ∈ [−3, 3] and the child
depth ≥ 1; LDSE only at depth ≤ 7 cut nodes; the LMR term is zero when either
score is invalid; every new spin is advertised, at its default, inside its
range; correction training still refuses singular-excluded nodes under
`CoreCorrTrainExcluded = 0`.

**Counters (diag), stride 1:** keep the existing `nmp_*`, `probcut_*`,
`singular_*`, `iir_*` names where semantics are unchanged; add
`nmp_skip_region`, `nmp_bound_shortcut`, `nmp_verify_region_fail`,
`probcut_tt_gate_reject`, `probcut_quiet_tt_reject`, `probcut_deeper_research`,
`probcut_tt_served`, `singular_extend_three`, `singular_ttmove_demoted`,
`ldse_applied`, `lmr_singular_term`. Decision-trace lines for every new
decision with its inputs. `AblationMask` bits unchanged: 2 null move,
3 ProbCut, 4 IIR + hindsight, 6 singular (with LDSE and the LMR term).

**SPSA surface (B.3.3, if the sweep curves), 35 coordinates:** NMP margin 5,
reduction 5, verify depth 1; ProbCut base, improving, depth divisor, adjust,
lerp, cap (6) and the TT-served margin only if that switch is on; singular
margin 1, double 4, triple 5, multi-cut lerp 1, LDSE 1; LMR term 3; and the
three LMR bases the term lands on (`CoreLmrQuiet`, `CoreLmrNoisy`,
`CoreLmrCutNode`), re-fitted from RAR-S75's theta. Curvature
sweep coordinates: `CoreNmpBase`, `CoreNmpREval`, `CoreProbcutBase`,
`CoreSingularMargin`, `CoreLmrSingularOffset`.

## Registered screens (B.3.2), against the head's numbers above

| Screen | Head | Floor | Target |
|---|---:|---:|---:|
| Branching 4–14 | 1.700 | window [1.55, 1.85] | inside |
| Nodes at depth 14 vs oracle | 1.90x | ≤ 2.2x | ≤ 1.7x |
| WAC at 100k | 221 | ≥ 215 | ≥ 226 |
| WAC at 400k | 260 | ≥ 255 | ≥ 263 |
| Oracle agreement at 300k | 41 | ≥ 39 | ≥ 43 |
| Canaries (`depthpv 10`) | 87/116 | none of the 87 lost | new passes recorded |
| Time-to-depth (bench 13 nodes / pooled-PGO NPS) | 1.00 | ≤ 1.10x | ≤ 1.00x |
| 2,000-game unfitted paired run | — | > −40 Elo | ≥ +5 proceeds to the sweep and SPSA; −40..+5 the review decides after the bit sweep |

The paired run governs (PLAN rule 8). The gate (B.3.4): Colosseum
`sprt-default` `[0,3]` nElo, cap 20,000 pairs (RAR-M10: a true +5 nElo
resolves in about 28,000 games, a true 0 in about the same), against the
accepted head at registration time — the B.2.4b head, or the B.2.7 head if
RAR-S78's gate accepts it first, in which case B.3's branch is rebased and
the zero-game screens and the paired run are repeated on the new base.

## B.3.1 implementation record (2026-09-23): T1 and T2 built, two premises contradicted, leaf returned to `RESEARCH`

**What exists.** T1 (`cbc602e`) and T2 (`2b9507b`) behind `b3proof`, off
by default. The off arm reproduces 7,185,678 / EBF 2.444 on all 40 lines
at both commits. The arm reports `+b3proof`. T3 (singular, LDSE, the LMR
term) and T4 (`CoreIirPolicy`) are **not built**: the stop rule for a false
premise applied after T2. The `ProofParams` struct holds the arm's
coordinates as `Core*` spins, `b3proof` only, so the accepted search
advertises none of them. The kept instrumentation, on both arms:
`nmp_cut_d3_6`, `nmp_cut_d7_12`, `nmp_cut_d13_plus`, `nmp_skip_region`,
`nmp_bound_shortcut`, `nmp_verify_region_fail`, `probcut_tt_gate_reject`,
`probcut_quiet_tt_reject`, `probcut_tt_served`, `probcut_deeper_research`,
and four not in the contract's list: `probcut_qpass_base0`,
`probcut_verify`, `probcut_verify_raised` and `probcut_verify_fail`. Those
four are what separated the two readings of P4.

**Choices where the contract was silent**, each within its rules:
1. The null move keeps Rarog's `depth >= 3` floor and consecutive-null
   guard, and ProbCut keeps its `depth >= 4` floor. The crossover note and
   P4's "TT gate alone" both assume these floors.
2. A verification that holds returns at least beta. The donor returns the
   null score, which in the lower-bound case is below beta. A proved
   fail-high must not read as a fail-low to the parent.
3. Inside a region a fail-high below beta, which would need verification,
   is dropped. Verifications never nest, so `nmp_min_ply` is zero after
   every one.
4. ProbCut stores the margin-shifted score, as the head does (storing the
   raw fail-high measured 5.55% slower to depth). A decisive score is stored
   and returned unshifted (R6). The entry's PV bit is `tt_pv`, which is false
   at the default population.
5. The TT-served shortcut sits inside ProbCut's population and entry guard,
   with no depth floor.
6. `potential_singularity` is false under ablation bit 6, so that bit
   removes every consequence of singular, including NMP's exclusion.

**Counters, `bench 13`, stride 1** (evidence `tools/results/b31-20260923/`,
ignored). The off arm reproduces every head number in *Known evidence*. The
arm's tree carries T1 and T2 only; its singular counters are the head's
in-loop singular on a changed tree, so P5 is not read.

| Counter | Off arm (head) | `b3proof` T1+T2 | Prediction |
|---|---:|---:|---|
| total / interior nodes | 7,185,678 / 3,869,032 | 6,709,407 / 4,149,978 | — |
| `nmp_attempt` | 488,901 | 54,484 (**−88.9%**) | P3 ≥ −60% ✓ |
| `nmp_cut` / conversion | 107,464 / 22.0% | 37,953 / **69.7%** | P3 ≥ 35% ✓ |
| `nmp_cut` d≤6 / 7–12 / 13+ | 85,759 / 21,442 / 263 | 24,912 / 13,000 / 41 | — |
| `nmp_verify_attempt` / `_fail` | 4,425 / 15 (0.34%) | 292 / 1 (0.34%) | P3 > 0.3% ✓ (numerically) |
| `nmp_skip_region` | — | **0** | the region's premise (Unknown 7) |
| `nmp_bound_shortcut` | — | 859 | — |
| `probcut_nodes` (+ gate rejects) | 355,294 | 65,236 (+ 361,848) | — |
| `probcut_attempt` | 78,390 | 35,049 (**−55.3%**) | P4 −10 to −30% ✗ (magnitude) |
| `probcut_cut` / attempt | 50,242 / 64.1% | 25,261 / 72.1% | P4 ≥ 60% ✓ |
| `probcut_qpass` → cut survival | 51,166 → **98.2%** | 25,467 → **99.2%** | P4 < 95% ✗ |
| of which base 0 (no real verification) | 16,626 | 14,587 | — |
| verified by ≥ 1 ply / failed | 34,540 / 924 (**97.3%** survive) | 9,845 / 206 (**97.9%**) | — |
| raised bound / re-searched at base | — | 1,467 / 3 | — |
| `singular_attempt` / interior | 189,351 / 4.9% | 243,691 / 5.9% | P5 not read (T3 unbuilt) |
| `iir_applied` / `rfp_cut` | 477,301 / 1,513,292 | 473,372 / 1,640,281 | — |

**Wire proofs** (tune build of the same source; each counter that read
zero or near zero was forced to move):

| Setting | Result |
|---|---|
| `CoreProbcutAdjust=300` | `probcut_deeper_research` 3 → 277, `probcut_verify_fail` 206 → 234 |
| `CoreNmpVerifyDepth=4` | verifications 292 → 36,396, 2 failures, `nmp_skip_region` 0 |
| `CoreNmpBase=0 CoreNmpDepth=0 CoreNmpVerifyDepth=4` | `nmp_skip_region` **9**, verification failures 107 of 69,924 (46 in a region) |
| Depth-3 floor removed (the one-line diff `depth >= 3` → `depth >= 1` in the `b3proof` null-move guard; reverted) | 398 verifications, 3 failures, 0 skips; at `CoreNmpVerifyDepth=4` 40,110, 3, 0 |

Binaries (release, `x86-64`, diag counters): off arm `a2d5cef6…728e`, arm
`02789256…9e08`, arm with `tune` `4152db79…ca6a`, the floor-removed
variant `57c02ddf…1bd8` (full SHA-256 in `hashes.txt` beside them). Recipe: `cargo build --release --features
diag[,tune],b3proof` at `2b9507b`, then `python
tools/diag/bench_counters.py --exe <exe> --depth 13 [--set …]`.

**Calibration against the frozen predictions** (so far; RAR-S79 is not
edited):
- **P3:** sign and magnitude right. Attempts −88.9%, conversion 69.7%. The
  mechanism is wrong for the verification leg: failures are 0.34% because
  there are fewer, deeper verifications, not because the region makes them
  real. The region refused nothing.
- **P4:** attempts missed in magnitude (−55% against −10 to −30%): the TT
  gate refuses 85% of the nodes that reach it. Survival missed through the
  mechanism, as Unknown 6 describes. The falsifier's wiring reading does not
  apply.

**Decision needed** (research, before T3):
- (a) Restate T2's expected mechanism as the TT gate, the lerp return and
  cheaper verification of thick margins. Re-register P4's survival leg on
  `probcut_verify_fail / probcut_verify`, which excludes the base-0 passes.
  Decide the region's fate. Then resume B.3.1 at T3 on the contract
  unchanged.
- (b) Give ProbCut a verification that can check, for example a real
  one-ply search where today there is none (base 0). That is a new
  mechanism and owes its own evidence first.
- (c) Keep the head's `depth − 4` verification and adopt only the gate and
  the return.

The implementation's recommendation is (a). The contract is implementable
as written, and T2's value is B.3.2's bit-3 question, not a counter's. The
region is inert but costs nothing. If an amendment drops it, the null move
reduces to the donor's shape without it, which is simpler. (b) is the only
option that tests the packet's original idea, and nothing measured yet says
Rarog needs it.

## Research amendment 2 (2026-09-23, after T1–T2's counters; before T3 and before any game)

**Unknown 6, ProbCut — option (a).** The packet misread the donor: its
verification is never deeper than `depth − 4 − improving`; it is *shallower*
against a raised bound when the qsearch margin is thick, and falls back to
the base depth when that fails. So T2's mechanism is restated as what it is:
the TT gate (both donors; it refuses 85% of the nodes that reach it), the
lerp return, and cheaper verification of thick margins (1,467 raised-bound
searches, 3 fallbacks). P4's survival leg is withdrawn as a falsifier — it
could not be reached by any T2 — and replaced by a read with no threshold:
`probcut_verify_fail / probcut_verify` (real verifications only; 97.9% on
the arm, 97.3% on the head). P4's attempts leg is a magnitude miss (−55%
against −10 to −30%), because the gate as both donors write it applies to
any valid TT score regardless of bound; it is donor-faithful and stays.
Option (b) is refused: a one-ply verification where base is 0 is a new
mechanism without evidence that Rarog needs it. Option (c) is refused: it
discards the donor's saving on thick margins for no evidence of harm.
Whether T2 is worth anything is B.3.2's bit-3 ablation and the paired run,
as the packet already said of T1.

**Unknown 7, the null-move region — kept, with its own wire check moved to
where it can fire.** `bench 13` has no node with remaining depth ≥ 16, so
the region cannot trigger there at its seed by construction, and at a low
verify depth RFP answers the shallow nodes first. The region is correct,
per-thread and costless when idle; its live range is game trees at STC
(root depth 16–20). B.3.2 adds a deeper probe: `go depth 20` on five
phase-4 positions with the `diag,b3proof` build, reading `nmp_skip_region`
and `nmp_verify_region_fail`; both still zero there sends the region to B.8
as an unconsumed mechanism. P3's verification-failure leg is recorded as met
for a different reason (fewer, deeper verifications), a mechanism miss.

**Implementation choices 1–7: accepted**, with two notes. Choice 2 (a
verified fail-high returns at least beta) is right and is Rarog's contract
from here: a proved fail-high is never read as a fail-low. Choice 7
(`ProofParams` beside `CoreParams`, `Core*` names, arm-only): fine for the
gate; B.8 folds it into `CoreParams` when the flag goes, and the tune build
must advertise every `Core*` spin (Assert-TuneSurface reads them from the
binary, so a missing one is refused before any game).

**T3 and T4, premises checked before resuming.**
- *Negative-extension share ≥ 20%* rests on the cut-node share among
  singular candidates, which nothing has measured: it becomes a read, not a
  falsifier. P5's activation band (±30% of 4.9%) already holds on the
  T1+T2 tree (5.9%).
- *TT-move demotion.* The donor demotes when `singular_score > tt_score`
  and the exclusion search made a move (`stack[ply].mv != NULL` after it).
  In Rarog the exclusion node cannot return early through a TT cutoff,
  razoring, RFP, null move or ProbCut — every one is guarded by
  `excluded.is_null()` — so the only early returns are rule draws and
  tablebase hits, and `clear_move` resets `stack[ply].mv` after each move,
  so the donor's test cannot be read. Demote on `singular_score > tt_score
  && !is_decisive(singular_score)` alone; a demotion after a rule-draw
  return is harmless, because the TT move is still searched. Recorded here
  as the one place T3 departs from the donor's letter.
- *The LMR term's `tt_move_score`* is the score the TT move returned at this
  node; the move loop has it when the TT move is searched first, and the
  term reads `VALUE_NONE` otherwise.
- T4 rests on no premise; its decision is B.3.2's screens.

**Resume B.3.1 at T3 on the contract, then T4.** The counters that gate the
resumption before any screen: singular attempts within ±30% of 4.9% of
interior nodes; `singular_extend_three` > 0 and < 5% of attempts;
`singular_multicut` 10–20%; `ldse_applied` > 0 at depth ≤ 7 only;
`lmr_singular_term` > 0; each wire proved by a setting that moves it.

## T3 record (2026-09-23): built on the contract and amendment 2, three P5 gates fail, leaf stopped before T4

*Resolved by Research amendment 3 at the end: (c) and (b), the bars kept, the sweep extended.*

**What exists.** T3 (`40dcd93`), behind `b3proof`, after T1 and T2.
- `potential_singularity` is computed once per node, after IIR and
  hindsight. The singular search runs before the move loop at the node's own
  `cut_node`, with margin `4·(Exact ? ceil(d/4) : d) + 4·d·(tt_pv && !PV)`
  and depth `(d − 1)/2`.
- Graded extensions go up to +3. Multi-cut returns the score lerped 0.4027
  toward beta and is never decisive. Demotion is on
  `singular_score > tt_score && !is_decisive(singular_score)`, which nulls
  the TT move for the picker. The −3 applies when `tt_score >= beta ||
  cut_node`.
- LDSE (+1 to the first move at a cut node, depth ≤ 7, estimate 11 below
  alpha) applies where there is no singular candidate. The root is excluded,
  and so is every node under ablation bit 6.
- The LMR term reads the TT move's returned score minus the exclusion score.
- The node's extension goes to its first move. A debug assertion holds it
  in [−3, 3] with a child depth ≥ 1 whenever it is non-zero.
- On the arm the head's in-loop singular block is compiled out, and so is
  `TtProbe::allows_singular` outside tests.
- Fifteen new `Core*` spins at their seeds.
- Tests: the extension is 1–3 and monotone in the gap; the multi-cut is
  never decisive, even against a losing beta; LDSE's gate; the LMR term
  needs both scores and is capped; a middlegame search runs singular
  searches and extends first moves; the proof arm's switches unwind and
  keep a legal PV.
- New counters: `singular_extend_three`, `singular_ttmove_demoted`,
  `ldse_applied` and `lmr_singular_term`, as listed in the contract. Three
  more split the singular population: `singular_attempt_cut_node`,
  `singular_extend_non_pv` and `singular_multicut_head_rule`.
- **T4 is not built.**

**Fingerprints.** The off arm reads 7,185,678 / EBF 2.444 with all 40 lines
identical, and `--no-default-features` reads 7,601,220 / 2.474. The
`b3proof` arm with T1–T3 reads **6,841,250 / EBF 2.422**.

**Counters, `bench 13`, stride 1, head against the arm (T1–T3)** (evidence
`tools/results/b31-20260923/t3/`, binaries hashed in `hashes.txt`):

| Counter | Head | Arm T1–T3 | Prediction | Reading |
|---|---:|---:|---|---|
| interior nodes | 3,869,032 | 4,403,153 | — | |
| `nmp_attempt` | 488,901 | 73,273 (−85.0%) | P3 ≥ −60% | ✓ |
| NMP conversion | 22.0% | 69.3% | P3 ≥ 35% | ✓ |
| NMP verification failures | 15 / 4,425 | 6 / 220 (2.7%) | P3 > 0.3% | ✓ (region refusals still 0) |
| `probcut_attempt` | 78,390 | 44,352 (−43.4%) | P4 −10 to −30% | ✗ magnitude (TT gate) |
| ProbCut cuts per attempt | 64.1% | 69.5% | P4 ≥ 60% | ✓ |
| verified survival (`1 − verify_fail/verify`) | 97.3% | 97.0% | read (amendment 2) | |
| `singular_attempt` / interior | 4.89% | **3.24%** | P5 ±30% of 4.9% | ✗ (5.44% at `depth >= 4`) |
| `singular_extend_three` / attempts | — | **21.0%** | P5 > 0 and < 5% | ✗ |
| `singular_multicut` / attempts | 14.9% | **32.2%** (19.3% under the head's rule) | P5 10–20% | ✗ |
| negative extension / attempts | 12.1% | 27.7% | read | |
| one / two / three plies | 49,737 / 37,238 / — | 2,599 / 15,265 / 29,915 | — | |
| demoted / attempts | — | 4.3% | — | |
| attempts at cut nodes | — | 89.5% | — | |
| `ldse_applied` | — | 360,690 (8.2% of interior) | P5 > 0 | ✓ |
| `lmr_singular_term` / `lmr_applied` | — | 26,092 / 2,155,576 | > 0 | ✓ |
| `iir_applied` / interior | 12.3% | 13.9% | — | |

**Wire proofs** (tune build of the same source, each against the default
row above):

| Setting | Moved |
|---|---|
| `CoreSingTripleBase=80` | three-ply 29,915 → 6,075, two-ply 15,265 → 30,580 |
| `CoreSingularMargin=12` / `=1` | multi-cut 45,882 → 17,244 / 148,927; attempts 79,220 / 384,082 |
| `SingularTtDepthMargin=0` | attempts 142,550 → 30,173 |
| `CoreLdseMargin=100` | `ldse_applied` 360,690 → 107,892 |
| `CoreLmrSingularOffset=300` / `CoreLmrSingularCap=0` | `lmr_singular_term` 26,092 → 1,694 / 0 |
| candidate floor `depth >= 4` (temporary diff, reverted) | attempts 5.44% of interior; three-ply 26.0%, multi-cut 28.6% of attempts |

**The arm's endgame suite fails one test.** Debug and release suites pass on
the off arm (329 and 330 tests). On the arm,
`kbnk_positions_are_driven_to_mate` fails in both profiles: mated within 90
plies at 2 of 5 budgets (40k, 50k and 60k nodes fail; 80k and 120k mate).
T1+T2 passed it. The attribution, one T3 part disabled per run by a
temporary edit (script and outputs in the evidence folder; one playout set
each, so this reads direction, not size):

| Variant | Budgets mated (40k, 50k, 60k, 80k, 120k) |
|---|---|
| T3 as built | 2 of 5 ✗ |
| multi-cut removed | pass |
| multi-cut only where the singular beta reaches beta (Rarog's rule) | pass |
| −3 removed | 0 of 5 ✗ |
| extensions capped at one ply | 1 of 5 ✗ |
| LMR term removed | 1 of 5 ✗ |
| demotion removed | 0 of 5 ✗ |
| LDSE removed | ✗, and a **stalemate** at 50k (a defect at any budget) |

Only the multi-cut changes the verdict, and Rarog's rule is enough. The
stalemate appears only in a variant nobody proposes, but B.3.2's bit-6
ablation removes LDSE together with the rest of singular, so that sweep
should run this test.

**Decision needed** (research, before T4). Each failed leg traces to one
term of the contract rather than to a defect:
- (a) Keep T3 donor-faithful. Re-register the three legs as reads with this
  calibration and resume at T4. Whether the shape helps is B.3.2's bit-6
  sweep and the paired run.
- (b) Revert the candidate floor to Rarog's `depth >= 4`, which restores the
  activation band. This is a one-line change.
- (c) Revert multi-cut to Rarog's rule (singular beta at beta), which brings
  multi-cut inside the band. This is also a one-line change.
- (d) Raise the non-PV extension bars. This is a seed question for the
  sweep, not a shape change.

The implementation's recommendation is **(c) plus (a)**. Multi-cut goes back
to Rarog's rule, because the donor's rule fails an existing regression test
and brings the multi-cut share outside its band. The candidate floor and the
extension bars stay donor-faithful, with their legs recorded as reads: those
misses follow arithmetically from the formulas, and the triple rate is what
the curvature sweep can move (`CoreSingTripleBase` moves it by a factor of
five). (b) remains the cheapest alternative if the amendment prefers Rarog's
floor.

## Research amendment 3 (2026-09-23, after T3's counters and the KBNK failure; before T4 and before any game)

**Multi-cut — (c), Rarog's rule, mandatory.** The donor's `singular_score >=
beta` accepts a fail-soft score from an exclusion search whose window was
`[singular_beta − 1, singular_beta]`, below beta; Rarog's `singular_beta >=
beta` requires the window itself to sit at or above beta, so the fail-high
proves the cut. On the arm the donor rule fails RAR-E14's KBNK discriminator
(2 of 5 budgets) and Rarog's passes; the single-part attribution runs point
at nothing else. Own evidence outranks the donor's shape. The lerp return
stays with Rarog's rule. Multi-cut's share under Rarog's rule reads 19.3%,
inside the registered band.

**Candidate floor — (b), Rarog's `depth >= 4` as the default, the donor's
as a switch.** `CoreSingularFloor` {0 = `depth >= 4` (default, the head's
own; restores the attempts band at 5.44%), 1 = `depth >= 5 + tt_pv`}, a
categorical for B.3.2's baseline/A/B screens beside the other five. The
band gate on attempts stays a gate at the default.

**Extension bars — (a) for the shape, (d) for the sweep.** Off the PV the
donor's own double bar is ≤ 0 as well (`−16·quiet − 16·|corr|/128`), so a
non-PV extension of at least two plies is the donor's design, not a seed
accident; 21% three-ply at the converted `CoreSingTripleBase` 16 is where
the seeds land. No shape change. `CoreSingTripleBase` joins the curvature
sweep as its sixth coordinate (it moves the triple rate five-fold), and the
three-ply and multi-cut legs of P5 become reads with their values recorded;
the attempts leg stays a gate at the default floor.

**LDSE-removed stalemate.** That configuration (T3 without LDSE) is not a
candidate; it is what ablation bit 6 produces together with the rest of
singular. B.3.2 runs `kbnk_positions_are_driven_to_mate` under each of the
four bits and records the result as a canary-class read (an ablated arm is
never gated); a stalemate on the *shipped* arm at any budget would be a
defect and is tested by the suite anyway.

**CI.** The arm is not in the CI matrix. B.3.1's close-out adds `b3proof`
to the feature matrix as B.2.1 added `b2core`: debug and release suites on
the arm and the bench agreement job reporting the arm's fingerprint beside
the default's (tooling commit, separate).

**Resume:** apply (c) and (b) (two one-line changes plus the switch and its
test), rerun both arms' suites — the arm must pass KBNK — re-read the
counters (attempts inside ±30% of 4.9% at the default floor, multi-cut
10–20%), then T4, then close.
